Connectivity test and default model update.

- Changed the default Groq model to `openai/gpt-oss-120b`.
- Added `ewtest`, an Alfred keyword for testing the configured LLM API key, base URL, and model.
- Added `test.sh` to the packaged workflow.
- Kept the OpenAI-compatible provider settings editable through Alfred's Configure Workflow panel.

No API key, `config.json`, or login state is included.
