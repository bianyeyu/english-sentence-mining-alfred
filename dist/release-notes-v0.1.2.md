Release alignment fix.

- Rebuilt the Alfred workflow package from the same source commit used for the GitHub release tag.
- Keeps the v0.1.1 Python launcher portability fix.
- Keeps the public README cleanup that removes personal local workspace paths.

No Codex login state, OpenAI API key, or local `config.json` is included.
